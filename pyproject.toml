[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "openhdo-sdk"
version = "0.1.0"
description = "Dependency-free Python SDK for OpenHDO devices and controllers"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["python"]
